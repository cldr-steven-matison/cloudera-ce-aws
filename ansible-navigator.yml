# Copyright 2026 Cloudera, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---

ansible-navigator:
  playbook-artifact:
    save-as: "runs/{playbook_name}-{time_stamp}.json"

  ansible-runner:
    artifact-dir: runs
    rotate-artifacts-count: 3

  logging:
    level: debug
    append: false

  ansible:
    inventory:
      entries:
        - inventory.yml

  execution-environment:
    enabled: true
    environment-variables:
      pass:
        - AWS_ACCESS_KEY_ID
        - AWS_SECRET_ACCESS_KEY
        - AWS_SESSION_TOKEN
        # Optional pre-existing SSH key
        # - SSH_PRIVATE_KEY_FILE
      set:
        ANSIBLE_SSH_CONTROL_PATH: "/dev/shm/cp%%h-%%p-%%r"
        ANSIBLE_CALLBACK_WHITELIST: "ansible.posix.profile_tasks"
        ANSIBLE_GATHERING: "smart"
        ANSIBLE_DEPRECATION_WARNINGS: false
        ANSIBLE_HOST_KEY_CHECKING: false
        ANSIBLE_SSH_RETRIES: 10
        # The license lives at ~/license.txt on this box. Set here (not passed from the shell) so a
        # run cannot start without it: 2026-09-21 (#358) a session that had not exported the
        # variable invented a placeholder license at another path.
        CDP_LICENSE_FILE: /home/tunas/license.txt
    # arm64-native EE: the 1.0.0 tag is multi-arch; pin the arch explicitly (no --platform needed on aarch64)
    image: ghcr.io/cloudera-labs/cloudera-ce-aws:1.0.0-arm64
    pull:
      policy: missing
    volume-mounts:
      - src: "/home/tunas/license.txt"
        dest: "/home/tunas/license.txt"
      # PATCH (#292): EE image drift — see patches/ and cloudera-ce-aws-runbook.md §2b
      - src: "/home/tunas/cloudera-ce-aws/patches/jdk_facts.py"
        dest: "/usr/share/ansible/collections/ansible_collections/cloudera/exe/plugins/modules/jdk_facts.py"
      - src: "/home/tunas/cloudera-ce-aws/patches/RedHat-pre.yml"
        dest: "/usr/share/ansible/collections/ansible_collections/cloudera/exe/roles/caddy/tasks/RedHat-pre.yml"
      # Optional pre-existing SSH key
      # - src: "${SSH_PRIVATE_KEY_FILE}"
      #   dest: "${SSH_PRIVATE_KEY_FILE}"
    container-options:
      - "--network=host"
